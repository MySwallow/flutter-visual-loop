name: flutter_visual_loop
description: Debug-only HTTP control plane for Flutter apps — powers automated UI restoration loops driven by Claude Code.
version: 0.1.0
homepage: https://github.com/MySwallow/flutter-visual-loop
repository: https://github.com/MySwallow/flutter-visual-loop

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
